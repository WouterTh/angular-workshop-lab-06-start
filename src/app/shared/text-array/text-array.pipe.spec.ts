import { TextArrayPipe } from './text-array.pipe';

describe('TextArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TextArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
